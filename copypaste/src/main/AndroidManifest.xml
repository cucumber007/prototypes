<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polyana.cucumber007.copypaste">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AbstractPopupActivity" />
        <activity android:name=".menu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".demo.FloatingActivity" />
        <activity android:name=".demo.photo_list.PhotoListActivity" />
        <activity android:name=".demo.tabs.DrawerActivity" android:exported="true" />
        <activity android:name=".demo.tabs.TabsActivity" android:exported="true" />
    </application>

</manifest>